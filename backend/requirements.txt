# ===== 核心运行时依赖（启动 API 所需）=====
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
# EmailStr 校验（注册接口邮箱格式校验依赖，缺失会导致应用启动失败）
email-validator>=2.1

# 数据库 / 缓存 / 向量库
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
greenlet>=3.0
alembic>=1.13
redis>=5.0
pymilvus>=2.4,<2.5
# pymilvus 2.4 依赖 pkg_resources，setuptools>=81 已移除该模块
setuptools<81

# 安全（JWT 鉴权）
pyjwt>=2.8
bcrypt>=4.1

# 工具
loguru>=0.7
python-multipart>=0.0.9
httpx>=0.27
minio>=7.2
pypdf>=4.0
python-docx>=1.1

# 测试
pytest>=8.2
pytest-asyncio>=0.23
