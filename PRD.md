# 产品需求文档（PRD）
## 基于 RAG 的企业级智能知识问答平台

---

## 1. 产品概述

本产品是一个**面向企业的智能知识问答平台**，基于检索增强生成（RAG）技术构建。企业可将内部文档（如制度规范、技术手册、项目报告、客户案例等）统一上传至平台，系统自动完成解析、向量化与索引构建。员工通过自然语言提问，即可获得**严格来源于内部资料**的准确答案，并附有引用来源。产品旨在打破部门信息孤岛，降低知识获取成本，提升全员工作效率。

---

## 2. 目标用户

| 用户类型 | 使用场景 |
| :--- | :--- |
| **企业内部员工** | 查询本部门文档，跨部门检索其他部门知识以协同工作 |
| **部门负责人** | 维护本部门知识库，向全公司共享最佳实践与业务规范 |
| **新入职员工** | 快速了解公司全业务线知识，无需逐个部门找人询问 |

---

## 3. 核心功能

### 3.1 文档管理
- 支持批量上传主流办公格式：PDF、DOCX、PPTX、XLSX、TXT、Markdown、HTML、CSV
- 后台异步解析，自动进行语义切片（保留标题层级）
- 文档列表展示、删除、重新向量化
- 支持父文档与子块关联，实现精确溯源

### 3.2 智能问答（核心）
- 基于 **混合检索（向量 + BM25）** 与 **重排序（Rerank）** 的 RAG 流水线
- 多轮对话，上下文记忆（可配置历史窗口长度）
- 答案**严格基于检索资料生成**，拒绝模型凭空编造
- 答案内嵌**引用来源**（文档名、页码、具体段落），点击可预览原文
- 支持流式输出（SSE），提供打字机式交互体验

### 3.3 高级检索策略（提升准确性）
- **HyDE（假设性文档嵌入）**：将问题扩展为假设答案后再检索，提升模糊问题召回率
- **查询改写**：对简略问题自动补全，提高检索命中率
- **多路召回融合**：同时执行向量检索和关键词检索，经 Rerank 精排后输出

### 3.4 知识库管理
- 支持创建多个独立知识库（对应不同部门、项目或产品线）
- 每个知识库可设置公开/私有，并可添加协作成员
- 支持跨知识库联合问答（一次提问检索多个知识库）

### 3.5 评估与质量监控
- 内置自动化评估工具，支持上传测试集（问题-标准答案-上下文三元组）
- 自动计算 **上下文相关度** 和 **答案正确率**，生成可视化报告
- 仪表盘展示：文档数量、向量存储量、平均响应时间、问答准确率趋势

### 3.6 管理后台
- 文档管理页：状态筛选、批量操作
- 用户行为日志：问答记录、反馈收集
- 系统配置：模型参数、检索参数、权限策略

---

## 4. 功能优先级（MoSCoW 法则）

| 优先级 | 功能模块 | 说明 |
| :--- | :--- | :--- |
| **Must have** | 文档上传（PDF/DOCX/TXT）、向量化入库、基础 RAG 问答（向量检索+生成）、引用显示、多轮对话 | 最小可行产品（MVP）核心闭环 |
| **Should have** | 混合检索（BM25+向量）、重排序（Rerank）、流式输出、多知识库管理、内置评估工具 | 提升体验与准确性 |
| **Could have** | HyDE、查询改写、父文档检索、PDF 表格/图片 OCR 支持、用户权限管理 | 增强竞争力，可后续迭代 |
| **Won't have** | 大模型微调、分布式集群、知识图谱自动构建 | 暂不纳入当前版本 |

---

## 5. 界面设计

### 5.1 对话主界面（类似 ChatGPT 风格）
- **左侧边栏**：知识库列表、历史会话记录、新建会话按钮
- **中间主区域**：对话消息流，用户问题靠右，助手回答靠左，回答下方展示**引用卡片**（点击展开来源详情）
- **底部输入框**：多行文本输入，支持 Enter 发送，Shift+Enter 换行
- **右上角**：当前知识库切换，系统状态指示

### 5.2 管理后台
- **文档管理页**：表格展示文档名、格式、上传时间、解析状态（成功/失败）、操作（删除、重试）
- **知识库管理页**：创建/编辑知识库，设置成员权限
- **评估面板**：上传测试集 JSON，点击“运行评估”，展示准确率、相关度等图表
- **系统仪表盘**：总文档数、总问答数、日均查询量、平均响应延迟

### 5.3 视觉风格
- 采用明亮/暗色双主题（默认跟随系统）
- 使用现代化 UI 组件库（如 Ant Design 或 MUI）
- 突出**可信度**：引用来源以显著样式展示，让用户一目了然

---

## 6. 技术栈建议

| 层级 | 组件 | 选型理由 |
| :--- | :--- | :--- |
| **后端框架** | Python 3.10+ / FastAPI | 异步高性能，自动生成 OpenAPI 文档，便于前后端联调 |
| **RAG 编排** | LangGraph | 支持复杂状态流转，便于实现 HyDE、多路召回等高级流程 |
| **大语言模型** | Qwen2.5-14B-Instruct（AWQ 量化）或 DeepSeek-V3 | 中文能力强，单卡 24GB 显存可运行，商业友好 |
| **Embedding 模型** | BAAI/bge-m3 或 text2vec-large-chinese | 中文语义理解 SOTA，支持稠密与稀疏向量 |
| **向量数据库** | Milvus（或 Pgvector + pgAdmin） | 支持百万级向量、混合检索、高可用，企业级标配 |
| **文档解析** | Docling（IBM）或 MinerU + PaddleOCR | 精准解析复杂表格、图片内文字，保留原始结构 |
| **前端框架** | React 18 + Next.js 14 + Tailwind CSS | 企业级应用首选，SSR 提升加载速度，Tailwind 便于快速定制 |
| **状态管理** | Zustand + React Query | 轻量且强大，管理全局状态和服务端缓存 |
| **部署方案** | Docker Compose + Nginx | 一键启动全栈服务，便于私有化交付 |
| **评估框架** | RAGAS（适配中文） | 自动化评估检索与生成质量，支撑产品迭代优化 |

---

## 7. 代码风格和架构模式

### 7.1 后端架构（六边形/清晰架构）

```
backend/
├── api/ # 路由层（FastAPI 端点）
├── core/ # 配置、常量、异常定义
├── domain/ # 业务实体（Document, Chunk, Answer）
├── application/ # 用例服务（上传、问答、评估）
├── infrastructure/ # 基础设施（向量库客户端、LLM 客户端、解析器）
└── graph/ # LangGraph 状态图定义
```


### 7.2 前端架构
- 使用 **Next.js App Router**，页面与 API 路由分离
- 组件设计遵循 **原子设计**（Atoms → Molecules → Organisms）
- 全局状态由 **Zustand** 管理，服务端数据由 **React Query** 缓存

### 7.3 代码规范
- Python：`black` + `isort` + `ruff`
- TypeScript/JavaScript：`ESLint` + `Prettier`
- 所有 API 输入输出必须显式定义 Pydantic / Zod Schema
- 关键函数需编写 docstring 和类型注解，便于团队协作

### 7.4 架构原则
- **依赖倒置**：高层模块不直接依赖底层实现，通过抽象接口解耦（便于替换模型或数据库）
- **事件驱动**：文档上传后发布事件，异步触发解析与向量化，避免阻塞主流程
- **可观测性**：接入结构化日志（Loguru）和性能指标（Prometheus，可选）

---

## 8. 限制条件和边界场景

| 限制/场景 | 处理策略 |
| :--- | :--- |
| **超大文档（>50MB）** | 限制单文件大小，或提供分片上传；解析超时则失败并提示 |
| **低质量扫描件（图片型 PDF）** | 若 OCR 无法识别，提示用户上传可复制文本的 PDF |
| **多语言混合内容** | 目前仅支持中文和英文，其他语言可能影响检索精度，可标注说明 |
| **并发访问压力** | 单机部署建议 ≤20 并发，如需更高可水平扩展 Milvus 和 LLM 服务 |
| **数据隐私与合规** | 所有数据存储在企业内网或私有云，不经过第三方 API；用户权限严格控制 |
| **答案不完整或错误** | 提供“反馈”按钮，收集用户纠错，用于后续优化检索和提示词 |
| **模型幻觉** | 通过系统提示词强制约束“仅根据资料回答”，且未检索到相关内容时明确告知用户 |

---

## 附：交付物要求
- 完整源代码（前后端分离）
- Docker Compose 配置文件（一键启动所有服务）
- 部署文档（README.md 含环境要求、启动步骤）
- API 文档（自动生成 Swagger UI）
- 演示数据（示例文档及测试集）