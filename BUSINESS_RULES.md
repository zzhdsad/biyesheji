# 业务规则文档（BUSINESS_RULES.md）

## 一、设计原则
1. **企业级管控优先**：使用者是企业员工，一切账号由管理员统一管控
2. **数据安全高于便捷**：宁可多一步操作，不泄露敏感信息
3. **操作可追溯**：所有数据变更操作必须留审计记录

---

## 二、账号与用户

| 规则 | 说明 |
| :--- | :--- |
| **不开放自助注册** | 登录页只有登录入口，无注册按钮 |
| **初始化管理员** | 首次启动自动创建 admin，首次登录强制改密 |
| **管理员创建员工** | 在“管理中心-用户管理”创建，支持批量导入 Excel |
| **离职处理** | **禁用账号**（不删除，保留数据，可恢复）<br>原因：删除会导致审计断层、文档归属丢失、误删无法挽回 |
| **密码重置** | 仅管理员可重置，重置后强制修改 |

**系统角色**：

| 角色 | 标识 | 权限 |
| :--- | :--- | :--- |
| **Admin** | 🔴 管理员 | 全部权限：用户管理、系统配置、审计日志、所有知识库 |
| **Member** | 🟡 成员 | 问答、上传文档（需知识库权限）、查看历史 |
| **Viewer** | 🟢 只读 | 仅问答和查看，不可上传、不可删除 |

---

## 三、知识库

| 规则 | 说明 |
| :--- | :--- |
| **创建** | 创建者自动为 Owner |
| **可见性** | **公开**：全员可查；**私有**：仅成员可查（非成员在列表和检索中均不可见） |
| **删除** | 仅 Owner，二次确认，移入回收站（默认 7 天，可配置 1-30 天） |

**知识库角色**：

| 角色 | 标识 | 权限 |
| :--- | :--- | :--- |
| **Owner** | 👑 拥有者 | 编辑知识库、添加/移除成员、转移所有权、删除知识库 |
| **Admin** | 🔴 管理员 | 管理文档、添加/移除普通成员 |
| **Editor** | 🟡 编辑者 | 上传、编辑、删除文档 |
| **Viewer** | 🟢 查看者 | 仅查看文档和提问 |

### 成员管理

| 操作 | 权限 | 说明 |
| :--- | :--- | :--- |
| **添加成员** | Owner / Admin | 按姓名/工号/邮箱搜索，选择角色后添加 |
| **批量添加** | Owner / Admin | 一次选择多个用户，统一分配角色 |
| **移除成员** | Owner / Admin | 二次确认，移除后立即失去访问权限 |
| **修改角色** | Owner / Admin | 在 Admin/Editor/Viewer 间切换 |
| **转移所有权** | 仅 Owner | 将 Owner 角色转移给其他成员 |

**边界规则**：
- 最后一个 Owner 不可被移除或降级，需先转移所有权
- 移除成员后，其历史文档和问答记录保留，不删除
- 添加成员时，已存在的用户自动跳过并提示

---

## 四、文档

| 规则 | 说明 |
| :--- | :--- |
| **支持格式** | PDF、DOCX、PPTX、XLSX、TXT、MD、HTML、CSV、**SQL/数据库导入** |
| **大小限制** | 默认 50MB（可配置），防止解析超时和内存溢出 |
| **批量上传** | 支持一次选择多个文件 |
| **解析状态** | 待解析 → 解析中 → 已完成 / 失败 |
| **默认筛选** | 进入页面默认显示“全部” |
| **删除** | 移入回收站，支持批量删除 |
| **重新解析** | 清除旧向量，重新执行解析、切片、向量化全流程 |

---

## 五、回收站

| 规则 | 说明 |
| :--- | :--- |
| **保留期** | 默认 7 天，管理员可配置 1-30 天 |
| **恢复** | 支持单个/批量恢复 |
| **彻底删除** | 仅管理员，二次确认，不可恢复 |
| **到期清理** | 系统自动清理过期内容 |

**注**：彻底删除 = 第二次删除（第一次为移入回收站）

---

## 六、智能问答

| 规则 | 说明 |
| :--- | :--- |
| **知识库选择** | 默认全选，可手动限定，记住用户偏好 |
| **多轮对话** | 默认携带最近 5 轮历史 |
| **新建会话** | 点击按钮立即创建并清空对话区 |
| **严格基于资料** | 禁止用模型通用知识回答 |
| **拒答机制** | 检索相关度 < 0.3 时拒答，提示“根据现有知识库中的资料，我无法回答该问题” |
| **引用标注** | 必须标注来源（文档名+页码），可点击溯源 |
| **引用过滤** | 只展示相关度 ≥ 0.3 的引用 |
| **流式输出** | 打字机效果 |

---

## 七、审计与日志

**记录操作**：用户管理、知识库增删改、文档增删改、问答记录、系统配置修改

**审计字段**：操作人、操作类型、目标对象、详情、IP、时间

**问答审计**：管理员可查看所有问答记录，按状态筛选（已答/拒答/低质量）

---

## 八、系统配置

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 模型部署方式 | — | 本地 / 远程 API |
| API Key | — | 远程模式必填，脱敏显示 |
| 检索 Top-K | 5 | 返回给 LLM 的片段数 |
| 相似度阈值 | 0.3 | 低于此值拒答 |
| Rerank 开关 | 开启 | 是否启用重排序 |
| HyDE 开关 | 开启 | 是否启用查询改写 |
| 回收站保留期 | 7 天 | 可配置 1-30 天 |
| 文件大小限制 | 50MB | 可配置 |

**未配置模型时**：对话页顶部提示“请先在管理中心配置模型参数”

---

## 九、交互规范

### 二次确认
- 删除知识库：弹窗确认
- 批量删除：弹窗确认，显示数量
- 彻底删除（回收站）：弹窗确认，提示“不可恢复”
- 禁用用户：弹窗确认

### 操作反馈
- 成功：Toast“操作成功”
- 失败：Toast 显示具体原因
- 加载中：按钮变灰 + Loading
- 空数据：空状态插图 + “暂无数据”

### 表单校验
- 必填项标记 `*`
- 实时校验 + 提交时统一校验
- 错误提示显示在输入框下方，红色文字

---

## 十、边界场景

| 场景 | 处理 |
| :--- | :--- |
| 超大文档（>50MB） | 拒绝上传，提示压缩或联系管理员 |
| 扫描件解析失败 | 提示上传可复制文本版本 |
| 检索无结果 | 拒答，提示上传相关文档 |
| 无权限访问 | 列表中不显示，检索时自动过滤 |
| 模型未配置 | 禁止提问，提示配置 |
| 数据库连接失败 | 显示“系统维护中” |